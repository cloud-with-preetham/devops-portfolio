name: SBOM Generation

on:
  workflow_call:

jobs:
  sbom-scan:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Generate SBOM
        uses: anchore/sbom-action@v0
